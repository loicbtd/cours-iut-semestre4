def encrypt(gap_size, message):
    encrypted_message = ""
    for char in message:
        if not char.isalpha():
            
        if  char.lower()
    return encrypted_message


def decrypt(gap_size, message):
    decrypted_message = ""
    return decrypted_message


def main():
    message = "Le code de Cesar"
    encrypted_message = "Mf dpef ef Dftbs"

    print(encrypt(1, message))
    # print(decrypt(1, encrypted_message))


main()
